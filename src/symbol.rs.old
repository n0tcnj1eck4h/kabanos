use std::collections::HashMap;

pub enum Symbol {}

pub struct SymbolDescriptor(usize);

pub struct SymbolTable {
    pub type_vec: Vec<Symbol>,
    pub type_map: HashMap<String, SymbolDescriptor>,
}

impl SymbolTable {}
